{
  "name": "evidence-vault-root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd Evidence_Vault && tsx server.ts",
    "build": "cd Evidence_Vault && npm run build",
    "preview": "cd Evidence_Vault && npm run preview",
    "clean": "cd Evidence_Vault && npm run clean",
    "lint": "cd Evidence_Vault && npm run lint",
    "start": "cd Evidence_Vault && npm start"
  },
  "dependencies": {},
  "devDependencies": {}
}
